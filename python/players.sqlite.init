CREATE TABLE players(
	id INTEGER,
	name TEXT,
	lastGame TEXT,
	wins INTEGER,
	mu INTEGER,
	sigma INTEGER,
	games INTEGER);

create TABLE playerHistoricalData (id TEXT, timestamp TEXT, mu REAL, sima REAL);
